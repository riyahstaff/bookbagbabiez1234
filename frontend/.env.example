# Copy this file to .env.local and adjust as needed. Never commit the real one.

# Where the browser reaches the FastAPI backend. The default already matches
# the docker-compose port mapping (backend published on the host's
# localhost:8000), so most setups don't need to override this at all.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
